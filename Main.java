class Cals{
    public static void main(String[] args)) throws Exception{ args:{}
        Scanner scanner = new Scanner(System,in);
    System.out.println('Введи арабские или римские числа')
            String expression = scanner.nextLine();

    System.out.printLn(parse(expression)));

    }

    public static String parse (String expression) throws Exception {
    int num1
    int num2
    String operator;
    String result;
    boolean Kirill;
    String[] operands = expression.split('[+\\-*/]'); expression "2+3"
    if(operands.length != 2) throw new Exception;
    oper = detectpOperation(expression);
    if (operator == null) throw new Exception

}